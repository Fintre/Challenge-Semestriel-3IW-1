/categorie/add:
  controller: Categories
  action: newCategorie

/media/add:
  controller: Medias
  action: newMedia

/comments:
  controller: Comments
  action: comments

/post/add:
  controller: Posts
  action: newPost

/settings:
  controller: SiteSettings
  action: settings

/user:
  controller: User
  action: user
